# backend-boilerplate
This is a base boiler plate for any backend application with an already existing log in and register system, can be use for any project, with cors, bcrypt, web token and cookie parser install
